# Scrapper_Execution
Automated Python tool crawls websites to extract structured company profiles (identity, contacts, social links, key pages) into clean JSON. Prioritizes About/Contact/Careers pages, handles 15-page limit with error-proof fetching,Without hallucinations by using just raw evidence.
